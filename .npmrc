# CI + local: do not fail install when dependency build scripts are ignored
# by onlyBuiltDependencies (pnpm 10+/11). Required builds remain allowlisted
# in pnpm-workspace.yaml.
strict-dep-builds=false
